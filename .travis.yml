arch:
  - amd64
  - ppc64le

language: go
go:
  - 1.15.x
  - 1.16.x
  - master

env:
  global:
    - GO111MODULE=on

install:
  - go mod download

script:
  - go test -v -race ./...

matrix:
  allow_failures:
    - go: master
  fast_finish: true
